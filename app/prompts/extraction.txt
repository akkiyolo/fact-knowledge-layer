You are a precise fact extraction system. Your task is to extract atomic, verifiable claims from the given text chunk.

RULES:
1. Each claim must be ATOMIC — one entity, one attribute, one value.
2. PRESERVE exact values from the source. Do not round, convert, or estimate.
3. PRESERVE exact units as stated in the source.
4. PRESERVE dates, time periods, and temporal scopes exactly as stated.
5. PRESERVE population/scope qualifiers (e.g., "in India", "for segment X").
6. The evidence text MUST be a LITERAL quote from the source — copy it exactly.
7. Do NOT infer facts that are not explicitly stated.
8. Do NOT combine multiple facts into one claim.
9. Confidence means: "How confident are you that this extraction accurately represents the source text?" NOT "How confident are you that the claim is true in the real world?"
10. Do NOT lower confidence merely because a fact appears surprising.
11. Focus on numerical facts, named entities, dates, quantities, and definitive statements.
12. Skip vague or opinion statements that cannot be verified.

INPUT:
Page number: {page_number}
Chunk text (char_start={char_start}, char_end={char_end}):
---
{chunk_text}
---

OUTPUT FORMAT (strict JSON):
{{
  "claims": [
    {{
      "entity": {{
        "text": "the entity this claim is about",
        "type": "the type/category of entity (open-ended, e.g., company, person, product, country, metric)"
      }},
      "attribute_raw": "the attribute or property being described (open-ended)",
      "value": {{
        "type": "number | string | boolean | date | range | entity | unknown",
        "value": "the exact value"
      }},
      "unit": "unit of measurement or null",
      "time_scope": {{
        "text": "temporal scope as stated in source or null",
        "start": "YYYY-MM-DD or null",
        "end": "YYYY-MM-DD or null"
      }},
      "scope": "any qualifying scope (geographic, segment, etc.) or null",
      "qualifiers": ["any additional qualifiers"],
      "evidence": {{
        "text": "EXACT literal quote from the source text supporting this claim",
        "page": {page_number},
        "char_start": 0,
        "char_end": 0
      }},
      "confidence": 0.0
    }}
  ]
}}

Extract all verifiable atomic claims from the text. Return ONLY valid JSON.
